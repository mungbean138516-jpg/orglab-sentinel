# The shipped prototype supports mock mode only.
DATA_MODE=mock

# Future backend-only values. Never rename these with a VITE_ prefix and never
# commit real credentials. Browser code must call a backend proxy instead.
SEC_USER_AGENT="OrgLab Sentinel contact@example.com"
FINNHUB_API_KEY="replace-on-server-only"

